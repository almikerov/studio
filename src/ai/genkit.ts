import {genkit} from 'genkit';
import {googleAI} from '@genkit-ai/googleai';

// This file is now mostly unused, but we keep it to avoid breaking imports.
// The AI logic has been moved to direct SDK calls in the flow files.

export const ai = genkit({
  plugins: [],
});
